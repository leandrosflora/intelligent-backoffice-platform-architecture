# Generated diagrams

SVG and PNG files in this directory are generated during CI and documentation publication by:

```bash
bash scripts/render-diagrams.sh
```

The canonical sources are the `.puml` files under `C4/`. Generated image files are not the source of truth.
